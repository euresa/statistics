function Y = sam_horner(A,X)
    %Takes a list of m points in X = [x1,...,xm] and 
    %coefficient list A and outputs the polynomial P(x) evaluated at those
    %points
    Y = ones(1,size(X));
    for (j = 1:length(X)){
        Y(j) = A(
        for (i = (length(A):1){ 
        
        end
    }
    end 

    %Returns array Y where Y(j) = P(xj)
